services:
  postgres:
    image: postgres:18
    container_name: todo-postgres
    restart: unless-stopped

    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}

    ports:
      - '${POSTGRES_PORT}:5432'

    volumes:
      - pgdata:/var/lib/postgresql

    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}']
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        DATABASE_URL: ${DATABASE_URL}

    container_name: todo-backend
    restart: unless-stopped

    env_file:
      - .env.docker

    environment:
      NODE_ENV: ${NODE_ENV}
      PORT: ${PORT}
      GATEWAY_URL_DEV: ${GATEWAY_URL_DEV}
      GATEWAY_URL_UAT: ${GATEWAY_URL_UAT}
      GATEWAY_URL_PROD: ${GATEWAY_URL_PROD}

    ports:
      - '${PORT}:${PORT}'

    depends_on:
      postgres:
        condition: service_healthy

volumes:
  pgdata:
